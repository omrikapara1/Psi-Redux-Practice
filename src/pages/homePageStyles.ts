import { makeStyles } from 'tss-react/mui';

export const useStyles = makeStyles()({
    root: {
      textAlign: "center",
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      height: '100vh',
      width: '100%'
    }
  });